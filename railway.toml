[build]
builder = "DOCKERFILE"
dockerfilePath = "ocr-worker/Dockerfile"
watchPatterns = ["ocr-worker/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
numReplicas = 1
