/**
 * Public discovery gate for Hourglass Conversations.
 *
 * Temporary retirement: the current Conversations experience is not shown
 * on the public website while a Case Studies replacement is prepared from
 * real Hourglass projects. Implementation and episode content stay in
 * `app/conversations` and `lib/conversations` for later reuse.
 *
 * Set to `true` to restore hub, footer, sitemap, and episode routes.
 * Do not rename this surface to Case Studies until that content exists.
 */
export const CONVERSATIONS_PUBLIC_DISCOVERY_ENABLED = false;
